import path from 'node:path';

// const pathToWordDir = path.join(process.cwd());

// export const PATH_DB = path.join(pathToWordDir, 'src/db', 'db.json');

export const PATH_DB = path.resolve('src/db/db.json');
